/**
 * Configuration constants for the Brain Visualization.
 */

export const NETWORK_TOPOLOGY = {
  NEURON_COUNT: 2200,
  MAX_CONNECTION_DISTANCE: 85,
  MAX_CONNECTIONS_PER_NEURON: 6,
  BRAIN_RADIUS: 480,
} as const;

export const BRAIN_SHAPE = {
  SCALE_X: 1.0,
  SCALE_Y: 0.8,
  SCALE_Z: 1.2,
  FISSURE_GAP_RATIO: 0.15,
  TEMPORAL_INDENT_THRESHOLD_RATIO: 0.2,
  TEMPORAL_INDENT_AMOUNT_RATIO: 0.1,
} as const;

export const ANIMATION_TIMING = {
  HIDE_DURATION_MS: 2000,
  SHOW_DURATION_MS: 4000,
  FORMATION_SPLIT_RATIO: 0.2,
  Y_ROTATION_SPEED: 0.5,
  X_OSCILLATION_AMPLITUDE: 0.15,
  X_OSCILLATION_FREQUENCY: 0.7,
  GLOBAL_TIME_SCALE: 0.0005,
} as const;

export const DRAG_ROTATION = {
  SENSITIVITY: 0.005,
  AUTO_RESUME_DAMPING: 0.95,
  VELOCITY_THRESHOLD: 0.0001,
  INERTIA_DECAY: 0.92,
} as const;

export const EXPLOSION = {
  MAX_RADIUS: 4000,
  EASING_POWER: 3,
  MAX_SUCTION_DELAY: 0.8,
  SUCTION_DELAY_POWER: 3,
} as const;

export const WAVE_PROPAGATION = {
  STEP_INTERVAL_MS: 60,
  GLOW_DURATION_MS: 500,
  SPAWN_INTERVAL_HIGH_MS: 800,
  SPAWN_INTERVAL_LOW_MS: 3000,
  HEMISPHERE_THRESHOLD: 20,
  PROPAGATION_PROBABILITY: 0.35,
} as const;

export interface MutableWavePropagationConfig {
  STEP_INTERVAL_MS: number;
  GLOW_DURATION_MS: number;
  SPAWN_INTERVAL_HIGH_MS: number;
  SPAWN_INTERVAL_LOW_MS: number;
  HEMISPHERE_THRESHOLD: number;
  PROPAGATION_PROBABILITY: number;
}

export let MUTABLE_WAVE_PROPAGATION: MutableWavePropagationConfig = {
  ...WAVE_PROPAGATION,
};

export const RENDERING = {
  NEURON_BASE_SIZE: 1.5,
  PERSPECTIVE_FOV: 400,
  CAMERA_Z_POSITION: -550,
  MIN_RENDER_SCALE: 0.05,
  CONNECTION_VISIBLE_PHASE: 0.3,
  GLOW_HALO_THRESHOLD: 0.3,
  HALO_SIZE_MULTIPLIER: 4,
  GLOW_EASING_POWER: 2,
  WAVE_SPAWN_PHASE_THRESHOLD: 0.1,
  STOP_ANIMATION_PHASE: 1.99,
} as const;

export const COLORS = {
  NEURON_ACTIVE: { r: 255, g: 255, b: 255 },
  GLOW_HALO: { r: 0, g: 255, b: 255 },
  NEURON_IDLE: { r: 100, g: 200, b: 255 },
  CONNECTION_ACTIVE: { r: 180, g: 255, b: 255 },
  CONNECTION_IDLE: { r: 100, g: 200, b: 255 },
} as const;

export const OPACITY = {
  NEURON_IDLE_FILL: 0.3,
  NEURON_IDLE_STROKE: 0.35,
  CONNECTION_IDLE: 0.15,
  CONNECTION_ACTIVE: 0.8,
  GLOW_HALO: 0.4,
  IMPLOSION_GLOW_THRESHOLD: 0.8,
} as const;

export const LINE_WIDTHS = {
  CONNECTION_ACTIVE: 0.8,
  CONNECTION_IDLE: 0.4,
  CUBE_WIREFRAME: 0.5,
  CUBE_PERSPECTIVE_OFFSET: 0.5,
} as const;
